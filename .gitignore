node_modules
.next
out
.env*.local
.DS_Store
*.tsbuildinfo
next-env.d.ts

# Supabase Access Token (설정용, 커밋 금지)
.supabase-token

# Supabase DB 비밀번호 (커밋 금지)
.supabase-db-password

# Vercel
.vercel
.env*

# Capacitor / 네이티브 빌드 산출물
android/app/build
android/build
android/.gradle
android/local.properties
android/app/release.keystore
ios/App/Pods
ios/App/build

# 안드로이드 서명 키 (커밋 금지 · 반드시 백업)
release.keystore
.android-signing

# 파이어베이스 안드로이드 설정 — 공개 저장소라 CI 비밀값으로 넣습니다
android/app/google-services.json
